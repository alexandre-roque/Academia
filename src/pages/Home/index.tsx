import { Cards } from "../../components/Cards";
import { InfoComponent } from "../../components/InfoComponent";

export const Home = () => {
    return (
        <>
            <InfoComponent />
            <Cards />
        </>
    );
};
